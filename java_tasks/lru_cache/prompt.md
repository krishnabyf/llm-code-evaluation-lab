# Task: LRU Cache

Implement a Java LRU cache with `get` and `put` in O(1) average time.

Requirements:

- Evict least recently used key when capacity is exceeded.
- Reading a key should mark it as recently used.
- Reject non-positive capacity.
- Include tests for eviction and update behavior.
